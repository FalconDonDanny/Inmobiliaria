# Sistema de Control - Inmobiliaria Pro
Este sistema permite gestionar el CRUD de propiedades utilizando Python 3 y MongoDB.

## Requisitos
* Python 3.x
* MongoDB ejecutándose en el puerto local 27017

## Instalación de dependencias
```bash
pip install -r requirements.txt
